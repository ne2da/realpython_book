import mypackage.agregate
# Импорт модуля agregate из пакета mypackage

print(mypackage.agregate.represent_final_output())
# При таком варианте импорта, обращение к функции должно осуществляться
# по полному квалификационному имени(пути)
